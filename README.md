# Simulador de turnos en clínica

## Requisitos
Antes de ejecutar el programa, asegúrate de instalar las librerías y dependencias necesarias para el proyecto usando el siguiente comando:

```bash
pip install -r requirements.txt
```